fastapi==0.111.0 # Use a specific version to ensure consistency
uvicorn==0.30.1 # ASGI server to run FastAPI
python-dotenv==1.0.1 # For local environment variables (optional, but good)
pytest
httpx
google-generativeai